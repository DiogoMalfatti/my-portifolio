import styled from 'styled-components'

export const HeaderStyle = styled.header`
  width: 100%;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 100;
  margin: 0.5rem 0 0.5rem 0;
  padding: 0 1rem 0 1rem;
  height: 4.5rem;
  background-color: var(--color-bar);
  box-shadow: 0 0 7.5px var(--color-blue);
`
